# TODO

## Resume Management Database Layer (Task 1)
- [x] Inspect existing Resume / ResumeVersion entities, enums, migration(s)
- [x] Align enums to required namespace (ensure single ResumeVersionStatus + ResumeVersionSource)
- [x] Add missing repositories (ResumeRepository, ResumeVersionRepository) with required methods
- [x] Verify build via `mvn clean compile`


